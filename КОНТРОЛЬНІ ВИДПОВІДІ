1. Приклади застарілих тегів у HTML5:
   - <center> для вирівнювання по центру
   - <font> для задання шрифту та кольору тексту
   - <strike> для перекреслення тексту
   - <frame> та <frameset> для визначення фреймів
   - <acronym> для скорочень, які можна перекласти
   - <applet> для вбудовування аплетів

2. Тег <main> використовується для визначення основного контенту сторінки, який містить основну інформацію, що потрібна користувачам. Тег <main> повинен містити тільки унікальний контент, а інші теги, такі як <header>, <footer>, <nav>, <aside>, тощо, повинні бути поза тегом <main>.

3. Основні теги для визначення семантичної структури документа HTML5:
   - <header> для визначення верхньої частини сторінки
   - <nav> для визначення блоку навігації
   - <article> для визначення окремого статті або контенту
   - <section> для визначення розділу або блоку контенту
   - <aside> для визначення бічної панелі або блоку контенту
   - <footer> для визначення нижньої частини сторінки

4. Браузер визначає версію HTML документа за допомогою декларації типу документа (Document Type Declaration або DOCTYPE). У HTML5 декларація типу документа має наступний вигляд: 

   `<!DOCTYPE html>`

   Якщо декларація типу документа відсутня, браузер буде сприймати документ як старшу версію HTML (наприклад, HTML4).

5. XHTML - це розширена версія HTML, яка має більш суворі вимоги до правильності синтаксису та розмітки. Документи XHTML повинні бути написані у валідному XML-форматі, тобто кожен елемент повинен мати закриваючий тег та всі атрибути мають бути взяті у лапки
